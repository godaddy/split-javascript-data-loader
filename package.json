{
  "name": "@godaddy/split-data-loader",
  "version": "0.0.1",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel lib -d lib --ignore 'lib/*.test.js'",
    "build-dry": "babel lib --ignore 'lib/*.test.js'",
    "prepublishOnly": "npm run build",
    "coverage": "nyc mocha 'lib/*.test.js'",
    "lint": "standard --fix --env mocha",
    "release": "standard-version --tag-prefix=''",
    "test": "mocha --exit --recursive lib"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/godaddy/split-javascript-data-loader.git"
  },
  "author": "GoDaddy Operating Company, LLC",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@babel/cli": "^7.7.5",
    "@babel/core": "^7.7.5",
    "@babel/preset-env": "^7.7.6",
    "chai": "^4.2.0",
    "mocha": "^6.2.2",
    "nyc": "^14.1.1",
    "standard": "^14.3.1",
    "standard-version": "^7.0.1"
  },
  "nyc": {
    "check-coverage": true,
    "lines": 100,
    "functions": 100
  }
}
